import React from "react";
import { View } from "react-native";

const AddressScreen = (address) => {
  const [loading, setLoading] = useState(true);

  return <View></View>;
};

export default AddressScreen;
